% This must be in the first 5 lines to tell arXiv to use pdfLaTeX, which is strongly recommended.
\pdfoutput=1
% In particular, the hyperref package requires pdfLaTeX in order to break URLs across lines.
\documentclass[11pt]{article}
\usepackage{amsmath}
\usepackage{tabularx} % Add this in the preamble
\usepackage{amssymb}
% Remove the "review" option to generate the final version.
\usepackage{ACL2023}

% Standard package includes
\usepackage{times}
\usepackage{latexsym}
\usepackage{algorithm}
\usepackage{algorithmic}
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{placeins}

% For proper rendering and hyphenation of words containing Latin characters (including in bib files)
\usepackage[T1]{fontenc}
% For Vietnamese characters
% \usepackage[T5]{fontenc}
% See https://www.latex-project.org/help/documentation/encguide.pdf for other character sets

% This assumes your files are encoded as UTF8
\usepackage[utf8]{inputenc}

% This is not strictly necessary, and may be commented out.
% However, it will improve the layout of the manuscript,
% and will typically save some space.
\usepackage{microtype}

% This is also not strictly necessary, and may be commented out.
% However, it will improve the aesthetics of text in
% the typewriter font.
\usepackage{inconsolata}
\usepackage{graphicx} 
\usepackage{float}
\usepackage{colortbl}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{multirow}

% If the title and author information does not fit in the area allocated, uncomment the following
%
%\setlength\titlebox{<dim>}
%
% and set <dim> to something 5cm or larger.

\begin{document}

\onecolumn

\begin{center}
{\Large\bfseries Pixel-level Certified Explanations via Randomized Smoothing
\\ Application to Medical Imaging Classifiers Project Report \par}
\author{Daniya Niazi \\
\texttt{7062343,dani00003@stud.uni-saarland.de} \\
Universit{\"a}t des Saarlandes}
\end{center}

\vspace{1em}
\section{Introduction}
This report documents the implementation of pixel-level certified explanations for medical imaging classifiers, based on \cite{anani2025pixel} and adapted to four datasets (ChestX-ray14, Brain MRI, APTOS, ISIC). The project focuses on producing attribution maps with provable per-pixel robustness guarantees using randomized smoothing and top-$K$ sparsification. In addition to standard medical datasets, a grid-based evaluation (Certified GridPG) is implemented to test whether certified important pixels align with a known target region.

The goals are: (i) build a unified attribution pipeline across methods, (ii) certify attributions at the pixel level under $\ell_2$-bounded input perturbations, (iii) evaluate robustness and localization, and (iv) document deviations from the original paper introduced by practical implementation choices.

\section{Background}
\subsection{Attribution Methods and Their Fragility}
Post-hoc attribution methods map an image to a heatmap of pixel importance for a predicted class. This work employs gradient-based (Integrated Gradients), activation-based (Grad-CAM), and perturbation-based (RISE, Occlusion) methods, plus LRP. While these methods are widely used, their outputs can be unstable: small, visually imperceptible perturbations can significantly change the heatmap without changing the prediction. This fragility motivates the need for certified explanations that provide robustness guarantees, not just qualitative visualization.
\subsection{XAI in Medical Imaging: Necessity and
Limitations}
Medical decisions depend on localized visual evidence (lesions, opacities, tumors), so attribution maps are often used for sanity checks and clinical interpretability. However, medical images are high-stakes and sensitive to noise, artifacts, and domain shift. Unstable explanations can mislead clinicians or hide spurious correlations. Certified attributions aim to reduce this risk by stating, for each pixel, whether its importance label is robust within a provable radius.


\section{Pixel-Level Certification for
Attributions on Medical Dataset}

\subsection{Overview}
The paper \cite{anani2025pixel} pipeline is implemented in two settings:
(i) standard medical datasets (ISIC, ChestX-ray14, Brain MRI, APTOS) using single-head classifiers, and
(ii) a grid-based setting for Certified GridPG, where each grid cell contains a different class image and the target cell is known. In both cases, attribution maps are computed, sparsified to top-$K$ pixels, and randomized smoothing is applied to certify the per-pixel labels.

The implementation follows a unified workflow: compute attributions, sparsify to top-$K$, apply randomized smoothing, and output a ternary certified map with labels 1 (important), 0 (unimportant), or abstain.

\subsection{Paper Implementation and Algorithm}

This section presents the complete certification workflow with formal algorithms for each stage: attribution computation, top-$K$ sparsification, randomized smoothing, and final certification.

\subsubsection{Overview}

Given image $x$, the pipeline: (1) compute attribution heatmap, (2) keep top $K\%$ pixels, (3) add noise and aggregate, (4) certify pixels as important/unimportant/abstain. Any perturbation $\|\delta\|_2 \le R = \sigma \Phi^{-1}(\tau)$ preserves certified labels.

\subsubsection{Algorithm 1: Top-$K$ Sparsification}

\begin{algorithm}[H]
\caption{Top-$K$ Sparsification}
\label{alg:topk_sparsification}
\begin{algorithmic}[1]
\REQUIRE Heatmap $h(x)$, sparsity $K\%$
\ENSURE Binary map $h_K(x) \in \{0,1\}$
\STATE Rank all pixels by attribution value
\STATE Mark top $K\%$ as 1, rest as 0
\RETURN $h_K(x)$
\end{algorithmic}
\end{algorithm}

\subsubsection{Algorithm 2: Randomized Smoothing}

\begin{algorithm}[H]
\caption{Randomized Smoothing}
\label{alg:randomized_smoothing}
\begin{algorithmic}[1]
\REQUIRE Image $x$, method $h$, $K$, $\sigma$, samples $n=100$
\ENSURE Probabilities $p_1(i), p_0(i)$ per pixel
\FOR{$j = 1$ to $n$}
    \STATE $x^{(j)} \leftarrow x + \epsilon$, $\epsilon \sim \mathcal{N}(0, \sigma^2)$
    \STATE $h_K(x^{(j)}) \leftarrow$ TopK$(h(x^{(j)}), K)$
    \STATE Count important/unimportant labels per pixel
\ENDFOR
\STATE $p_1(i) = \text{count}_1[i] / n$, $p_0(i) = \text{count}_0[i] / n$
\RETURN $p_1, p_0$
\end{algorithmic}
\end{algorithm}

\subsubsection{Algorithm 3: Certification}

\begin{algorithm}[H]
\caption{Certification}
\label{alg:certification}
\begin{algorithmic}[1]
\REQUIRE Probabilities $p_1(i), p_0(i)$, threshold $\tau=0.75$
\ENSURE Certified map $c(x)$
\FOR{each pixel $i$}
    \IF{$p_1(i) > \tau$} $c(x)_i \leftarrow 1$
    \ELSIF{$p_0(i) > \tau$} $c(x)_i \leftarrow 0$
    \ELSE $c(x)_i \leftarrow$ abstain
    \ENDIF
\ENDFOR
\RETURN $c(x)$
\end{algorithmic}
\end{algorithm}

\subsubsection{Settings}

All experiments use: $\sigma = 0.15$, $n = 100$, $\tau = 0.75$, $K \in \{5\%, 25\%, 50\%\}$, certified radius $R \approx 0.101$.


\subsection{Implementation Notes}
Two adjustments: (1) explicit pixel ranking for top-$K$, (2) multi-head DiFull wrapper for grid evaluation.

\subsection{Dataset Details and Characteristics}

This section describes the medical imaging datasets used in this seminar project for evaluating pixel-level certified explanations. The datasets span multiple imaging modalities and clinical tasks, enabling an assessment of certified attribution methods across diverse visual characteristics, class structures, and safety-critical scenarios. For each dataset, the imaging modality, classification task, and clinical relevance are outlined, with representative sample images provided.

\subsubsection{ChestX-ray14}

The ChestX-ray14 dataset consists of frontal chest radiographs acquired using X-ray transmission imaging. In this project, the task is formulated as a binary classification problem distinguishing pneumonia from normal cases. Chest X-rays are among the most frequently used diagnostic imaging tools worldwide, making them a high-impact and safety-critical application domain. Representative samples from the dataset are shown in Figure~\ref{fig:chestxray}.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\linewidth]{data sample/chestsample.png}
    \caption{Representative samples from the ChestX-ray14 dataset showing normal and pneumonia cases.}
    \label{fig:chestxray}
\end{figure}

\subsubsection{Brain MRI Tumor Dataset}

The Brain MRI Tumor dataset consists of T1-weighted magnetic resonance images, released under a Creative Commons license. The dataset is pre-cleaned, resized, and augmented, and supports a four-class classification task: glioma, meningioma, pituitary tumor, and normal brain tissue. The class distribution in the training set is approximately balanced. Representative samples are shown in Figure~\ref{fig:brainmri}.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\linewidth]{data sample/brain sample.png}
    \caption{Sample images from the Brain MRI Tumor dataset illustrating different tumor types and normal anatomy.}
    \label{fig:brainmri}
\end{figure}

\subsubsection{APTOS 2019 (Diabetic Retinopathy)}

The APTOS 2019 dataset consists of color fundus photographs captured using retinal imaging devices. The task is a five-class ordinal classification corresponding to diabetic retinopathy (DR) severity: Grade 0 (no DR), Grade 1 (mild), Grade 2 (moderate), Grade 3 (severe), and Grade 4 (proliferative).

The training data reflects real-world clinical prevalence, with approximately 49\% normal cases, followed by moderate (20\%), mild (13\%), severe (11\%), and proliferative (7\%) DR. Diabetic retinopathy is a leading cause of blindness, and early detection depends on identifying subtle vascular and color-based features. This makes fundus images particularly suitable for analyzing how certified explanations behave with respect to color and fine-grained texture cues. Representative samples are shown in Figure~\ref{fig:aptos}.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\linewidth]{data sample/fundus.png}
    \caption{Representative fundus images from the APTOS 2019 dataset across different diabetic retinopathy severity levels.}
    \label{fig:aptos}
\end{figure}

\subsubsection{ISIC Skin Lesion Dataset}

The ISIC Skin Lesion dataset contains dermoscopic images captured using magnified color photography. The dataset supports a seven-class classification task: melanoma, melanocytic nevus, basal cell carcinoma, actinic keratosis, benign keratosis, dermatofibroma, and vascular lesions.

The dataset exhibits significant class imbalance, with melanocytic nevi comprising approximately 60\% of samples, while melanoma accounts for roughly 10\%. Dermoscopic images present substantial visual complexity due to color variation, texture patterns, and common artifacts such as hair or rulers. To ensure computational feasibility and balanced evaluation, the dataset was subsampled to a maximum of 500 images per class. Representative examples are shown in Figure~\ref{fig:isic}.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\linewidth]{data sample/isic sample.png}
    \caption{Example dermoscopic images from the ISIC dataset illustrating diverse lesion types and visual variability.}
    \label{fig:isic}
\end{figure}

\subsubsection{Dataset Configuration}

All datasets were split into training, validation, and test sets using an 80/10/10 ratio. Images from all datasets were resized to a uniform resolution of $224 \times 224$ pixels to ensure consistency across models and attribution methods.

\begin{table}[h!]
\centering
\caption{Summary of datasets used in the certification experiments.}
\begin{tabularx}{\linewidth}{|X|X|X|X|X|}
\hline
\textbf{Dataset} & \textbf{Image Modality} & \textbf{Train-Test-Val Split} & \textbf{Image Size} \\
\hline
ChestX-ray14 & X-ray (grayscale) & $80-$10-$10\% & $224 \times 224$ \\
\hline
Brain MRI Tumor & MRI & 80-$10-$10\% & $224 \times 224$ \\
\hline
APTOS 2019 & Fundus photography& $80-$10-$10\% & $224 \times 224$ \\
\hline
ISIC Skin Lesion & Dermoscopic images & $80-$10-$10\% & $224 \times 224$ \\
\hline
\end{tabularx}
\label{tab:dataset_summary}
\end{table}


\newpage
\subsection{Model Architecture Selection and Training Results}

This section presents a consolidated comparison of convolutional neural network (CNN) architectures evaluated across multiple medical imaging datasets. Instead of reporting dataset-specific results in separate paragraphs, validation accuracy is summarized in a single table to facilitate direct comparison across models and modalities. This analysis informs the selection of representative architectures for subsequent certified attribution experiments.

\subsubsection{Model Architectures}

Six CNN architectures were evaluated: ResNet-18, ResNet-50, DenseNet-121, EfficientNet-B0, EfficientNet-B1, and MobileNetV2. All models were initialized with ImageNet-1k pre-trained weights and fine-tuned on the medical datasets using the Adam optimizer with a learning rate of $1 \times 10^{-4}$, batch size 32, and early stopping with a patience of five epochs. Standard data augmentation was applied, including rotations ($\pm15^\circ$), horizontal flipping, and color jitter for RGB datasets only.

\subsubsection{Validation Accuracy Across Datasets}

Table~\ref{tab:val_acc_models} reports the validation accuracy of each model across the four medical imaging datasets. For each dataset, the highest validation accuracy is highlighted. While EfficientNet-B1 consistently achieves the strongest predictive performance, DenseNet-121 and ResNet-based architectures remain competitive. Simpler models such as ResNet-18 and MobileNetV2 are later favored for detailed certification analysis due to their computational efficiency and more stable attribution behavior.
\begin{table}[H]
\centering
\caption{Validation accuracy (\%) of CNN architectures across medical imaging datasets. Best-performing model per dataset is highlighted.}
\small
\setlength{\tabcolsep}{4pt} % default is ~6pt
\renewcommand{\arraystretch}{1.15}
\begin{tabularx}{\linewidth}{|X|c|c|c|c|c|c|}
\hline
\textbf{Dataset} 
& \textbf{ResNet-18} 
& \textbf{ResNet-50} 
& \textbf{DenseNet-121} 
& \textbf{EfficientNet-B0} 
& \textbf{EfficientNet-B1} 
& \textbf{MobileNetV2} \\
\hline
ChestX-ray14 
& 88.4 & 89.1 & 89.8 & 89.4 & \cellcolor{gray!25}\textbf{91} & 87.6 \\
\hline
Brain MRI Tumor 
& 90.9 & 91.7 & 92.3 & 92.0 & \cellcolor{gray!25}\textbf{92.7} & 90.1 \\
\hline
APTOS 2019 (DR) 
& 86.8 & 87.6 & 88.1 & 87.9 & \cellcolor{gray!25}\textbf{88.7} & 86.2 \\
\hline
ISIC Skin Lesion 
& 84.4 & 85.5 & 86.2 & 85.9 & \cellcolor{gray!25}\textbf{86.9} & 83.8 \\
\hline
\end{tabularx}
\label{tab:val_acc_models}
\end{table}


\begin{figure}[H]
\centering
\begin{subfigure}{0.22\linewidth}
    \centering
    \includegraphics[width=\linewidth]{confusion matrix/brain_mri_confusion_matrix.png}
    \caption{Sample 1}
\end{subfigure}
\hfill
\begin{subfigure}{0.22\linewidth}
    \centering
    \includegraphics[width=\linewidth]{confusion matrix/chestxray_confusion_matrix.png}
    \caption{Sample 2}
\end{subfigure}
\hfill
\begin{subfigure}{0.22\linewidth}
    \centering
    \includegraphics[width=\linewidth]{confusion matrix/confusion_matrix.png}
    \caption{Sample 3}
\end{subfigure}
\begin{subfigure}{0.22\linewidth}
    \centering
    \includegraphics[width=\linewidth]{confusion matrix/fundus_confusion_matrix.png}
    \caption{Sample 3}
\end{subfigure}

\caption{Confusion Matrix of ResNet18 model across the datasets}
\label{fig:brain_attr}
\end{figure}



\subsubsection{Model Selection for Certification Experiments}

Although EfficientNet-B1 achieves the highest validation accuracy across all datasets, ResNet-18 was selected as the primary model for extensive certification experiments (Experiment 2). This choice reflects a trade-off between predictive performance, computational efficiency, and attribution interpretability, as illustrated on representative samples (e.g., 10 ISIC images shown in Figure~\ref{fig:isic}). In addition, ResNet-18, MobileNetV2, DenseNet-121, and EfficientNet-B1 were evaluated on a limited subset of samples ($N=5$ per dataset) to analyze architectural differences in certified attribution behavior.


\newpage
\subsection{Attribution Methods: Settings and Implementation}

This section describes the attribution methods used in this work. For each method, the intuition is briefly explained, a simplified pseudo-algorithm is presented, and the concrete hyperparameter settings used in the implementation are described. All methods produce pixel-level heatmaps compatible with the certification framework.

% ---------------------------------------------------------
\subsubsection{Integrated Gradients}
% ---------------------------------------------------------

\paragraph{Method intuition.}
Integrated Gradients (IG) estimates pixel importance by averaging gradients along a straight-line path from a baseline image to the input image, reducing sensitivity to local gradient noise.

\begin{algorithm}[H]
\caption{Integrated Gradients (IG)}
\label{alg:ig}
\begin{algorithmic}[1]
\REQUIRE Model $f_\theta$, input $x$, baseline $x'$, target class $c$, steps $N$
\ENSURE Attribution map $h(x)$
\FOR{$\alpha = 0$ to $1$ in $N$ steps}
    \STATE $x_\alpha \leftarrow x' + \alpha (x - x')$
    \STATE Compute gradient $\nabla_x f_\theta(x_\alpha)_c$
\ENDFOR
\STATE Average gradients and compute $(x - x') \odot \nabla_x$
\STATE Sum across channels and normalize
\RETURN $h(x)$
\end{algorithmic}
\end{algorithm}

\paragraph{Implementation details.}
Integrated Gradients uses a zero-valued (black) image as the baseline and approximates the path integral using 50 uniformly spaced interpolation steps. Gradients are accumulated along the interpolation path, multiplied by the input difference, and summed across channels to obtain a pixel-level heatmap. The final attribution map is normalized to the range $[0,1]$ for visualization and certification compatibility.

\paragraph{Remarks.}
Integrated Gradients produces smooth and spatially coherent attribution maps due to gradient averaging along the interpolation path. This implicit smoothing improves robustness to small input perturbations, which explains its consistently strong certification performance across datasets. However, the method requires multiple forward and backward passes per image, making it more computationally expensive than single-pass gradient methods.

% ---------------------------------------------------------
\subsubsection{Grad-CAM}
% ---------------------------------------------------------

\paragraph{Method intuition.}
Grad-CAM highlights spatial regions by weighting convolutional feature maps using gradients of the target class, producing coarse but stable explanations.

\begin{algorithm}[H]
\caption{Grad-CAM}
\label{alg:gradcam}
\begin{algorithmic}[1]
\REQUIRE Model $f_\theta$, target conv layer $L$, input $x$, class $c$
\ENSURE Heatmap $h(x)$
\STATE Store activations $A^k$ from layer $L$
\STATE Compute gradients $\partial f_\theta(x)_c / \partial A^k$
\STATE Compute weights $\alpha_k = \text{mean}(\nabla A^k)$
\STATE $h(x) = \sum_k \alpha_k A^k$
\STATE Apply ReLU and upsample
\RETURN $h(x)$
\end{algorithmic}
\end{algorithm}

\paragraph{Implementation details.}
In our implementation, Grad-CAM targets the final convolutional block (\texttt{layer4}) of the network. The resulting low-resolution activation map is upsampled to $224 \times 224$ using bilinear interpolation and passed through a ReLU operation to remove negative contributions. This produces a coarse but spatially stable attribution map aligned with the input image.

\paragraph{Remarks.}
Grad-CAM provides stable but coarse explanations, as relevance is derived from low-resolution convolutional feature maps. The inherent spatial downsampling acts as an implicit regularizer, improving robustness under certification. However, this comes at the cost of localization precision, particularly for small or fine-grained structures in medical images.

% ---------------------------------------------------------
\subsubsection{Layer-wise Relevance Propagation (LRP)}
% ---------------------------------------------------------

\paragraph{Method intuition.}
LRP redistributes the prediction score backward through the network while conserving total relevance, yielding sharp and localized explanations.

\begin{algorithm}[H]
\caption{Layer-wise Relevance Propagation (LRP)}
\label{alg:lrp}
\begin{algorithmic}[1]
\REQUIRE Model $f_\theta$, input $x$, target class $c$
\ENSURE Relevance heatmap $h(x)$
\STATE Forward pass to obtain prediction
\STATE Initialize relevance at output neuron
\FOR{each layer from output to input}
    \STATE Redistribute relevance using $\epsilon$-rule
\ENDFOR
\STATE Sum relevance across channels
\RETURN $h(x)$
\end{algorithmic}
\end{algorithm}

\paragraph{Implementation details.}
LRP is implemented using the Zennit framework with the $\epsilon$-rule ($\epsilon = 10^{-6}$) to ensure numerical stability during relevance propagation. Channel-wise relevance maps are summed and converted to absolute values to obtain pixel-level attributions. Importantly, normalization is applied only after certification to preserve the relative ranking of relevance scores.

\paragraph{Remarks.}
LRP generates sharp and highly localized attribution maps, often aligning well with visually salient regions. However, relevance propagation is sensitive to activation discontinuities (e.g., ReLU), leading to high variability under small perturbations. Without explicit robustness constraints, this sensitivity limits certification performance despite strong qualitative results on selected samples.

% ---------------------------------------------------------
\subsubsection{RISE}
% ---------------------------------------------------------

\paragraph{Method intuition.}
RISE estimates pixel importance by randomly masking parts of the image and measuring how the model’s prediction changes.

\begin{algorithm}[H]
\caption{RISE}
\label{alg:rise}
\begin{algorithmic}[1]
\REQUIRE Model $f_\theta$, input $x$, class $c$, masks $N$
\ENSURE Heatmap $h(x)$
\FOR{$i = 1$ to $N$}
    \STATE Sample random mask $M_i$
    \STATE Compute logit $f_\theta(x \odot M_i)_c$
    \STATE Accumulate $f_\theta(x \odot M_i)_c \cdot M_i$
\ENDFOR
\STATE Normalize attribution map
\RETURN $h(x)$
\end{algorithmic}
\end{algorithm}

\paragraph{Implementation details.}
RISE generates 500 random binary masks at a coarse resolution of $7 \times 7$, which are upsampled to the input image size using bilinear interpolation. Each masked image is forwarded through the model, and the target class logit is used as the weighting score for attribution accumulation. The final attribution map is normalized after aggregating contributions from all masks.

\paragraph{Remarks.}
Due to its stochastic nature, RISE exhibits high variance. Under certification, this variance is amplified, resulting in low robustness (20--28\%). The method is also computationally expensive due to the large number of forward passes.

% ---------------------------------------------------------
\subsubsection{Occlusion}
% ---------------------------------------------------------

\paragraph{Method intuition.}
Occlusion measures importance by systematically hiding image patches and observing the drop in prediction confidence.

\begin{algorithm}[H]
\caption{Occlusion}
\label{alg:occlusion}
\begin{algorithmic}[1]
\REQUIRE Model $f_\theta$, input $x$, class $c$
\ENSURE Heatmap $h(x)$
\FOR{each patch location}
    \STATE Replace patch with baseline value
    \STATE Measure logit drop $\Delta$
    \STATE Assign $\Delta$ to patch
\ENDFOR
\STATE Upsample attribution map
\RETURN $h(x)$
\end{algorithmic}
\end{algorithm}

\paragraph{Implementation details.}
Occlusion uses square patches of size $16 \times 16$ with a stride of 8 pixels, resulting in overlapping occlusions across the image. Occluded regions are replaced with the mean pixel value of the image to avoid introducing artificial artifacts. The resulting low-resolution attribution map is then upsampled to full image resolution using bilinear interpolation.

\paragraph{Remarks.}
Occlusion-based methods are highly sensitive to patch alignment. Small input shifts change which regions are occluded, leading to unstable attribution maps. Combined with high computational cost, this explains the low certification rates (12--22\%) observed.


% =========================================================
\subsection{Attribution Visualization Across Datasets}
% =========================================================

This section presents qualitative attribution visualizations across datasets. For each dataset, two representative samples are selected from the ResNet-18 model. Attribution maps are generated using Integrated Gradients, Grad-CAM, RISE, Occlusion, and LRP.

\subsubsection{Brain MRI Tumor}
\begin{figure}[H]
\centering

\begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=\linewidth]{attribution sample/brainatt1resnet18.png}
    \caption{Sample 1}
\end{subfigure}

\vspace{1em} % control vertical gap

\begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=\linewidth]{attribution sample/brainattr2resnet18.png}
    \caption{Sample 2}
\end{subfigure}

\caption{Attribution visualizations for Brain MRI samples using ResNet-18. 
Each row corresponds to one sample. Within each image (left to right): input image, Integrated Gradients, Grad-CAM, RISE, Occlusion, and LRP.}
\label{fig:brain_attr}
\end{figure}

\subsubsection{Chest X-ray}

\begin{figure}[H]
\centering

\begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=\linewidth]{attribution sample/chestattrsample1.png}
    \caption{Sample 1}
\end{subfigure}

\vspace{1em} % control vertical gap

\begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=\linewidth]{attribution sample/chestattrsample2.png}
    \caption{Sample 2}
\end{subfigure}

\caption{Attribution visualizations for an ChestXray images using ResNet-18.}
\label{fig:aptos_attr}
\end{figure}




\subsubsection{APTOS 2019 (DR)}

\begin{figure}[H]
\centering

\begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=\linewidth]{attribution sample/aptosatt1sample1.png}
    \caption{Sample 1}
\end{subfigure}

\vspace{1em} % control vertical gap

\begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=\linewidth]{attribution sample/aptosatt1sample2.png}
    \caption{Sample 2}
\end{subfigure}
\caption{Attribution visualizations for an APTOS 2019 (DR) sample using ResNet-18.}
\label{fig:isic_attr}
\end{figure}

\subsubsection{ISIC Skin Lesion }

\begin{figure}[H]
\centering

\begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=\linewidth]{attribution sample/skinattr1.png}
    \caption{Sample 1}
\end{subfigure}

\vspace{1em} % control vertical gap

\begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=\linewidth]{attribution sample/skinsample2.png}
    \caption{Sample 2}
\end{subfigure}
\caption{Attribution visualizations for an ISIC Skin Lesion sample using ResNet-18.}
\label{fig:isic_attr}
\end{figure}

\newpage


\subsection{Certification Visualization Across Datasets with ResNet-18}

This section presents certified attribution visualizations across all datasets. For each dataset, two representative validation samples are selected using the ResNet-18 model. Attribution maps are generated using Integrated Gradients, Grad-CAM, RISE, Occlusion, and LRP, and subsequently evaluated under a unified robustness certification framework.

\subsubsection{Standard Certification Framework and Hyperparameter Settings}

All certification experiments follow a consistent randomized smoothing protocol across datasets and attribution methods to ensure fair and comparable evaluation. Given an input image $x$ and its attribution heatmap $h(x)$, robustness is assessed by injecting Gaussian noise into the input and analyzing the stability of the resulting attribution maps.

Specifically, for each image, we generate $N = 100$ noisy samples of the form
\[
x^{(i)} = x + \epsilon^{(i)}, \quad \epsilon^{(i)} \sim \mathcal{N}(0, \sigma^2 I),
\]
where $\sigma = 0.15$ is fixed for all experiments. For each noisy sample, the attribution method is recomputed, producing a set of noisy attribution maps that are subsequently aggregated into a smoothed sparsified (SS) attribution.

Certification is performed by thresholding the smoothed attribution according to a confidence parameter $\tau = 0.75$. A pixel is considered \emph{certified} if its attribution label remains consistently above the threshold across noisy samples within the certified radius induced by $\sigma$. Pixels that do not meet this consistency requirement are marked as abstained. Certification is evaluated at three sparsity levels corresponding to the top $K=\{5\%, 25\%, 50\%\}$ most salient pixels.

The same certification parameters ($\sigma$, $N$, $\tau$, and $K$ values) are applied uniformly across all datasets and models. This standardized setup ensures that observed differences in certification behavior are attributable to dataset characteristics or attribution methods rather than hyperparameter tuning.

\FloatBarrier
\subsubsection{Brain MRI Tumor}
\begin{figure}[H]
\centering

\begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=0.8\linewidth]{certifcation/mri/resnet18_img5_paper_style.png}
    \caption{Sample 1}
\end{subfigure}

\begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=0.8\linewidth]{certifcation/mri/resnet18_img59_paper_style.png}
    \caption{Sample 2}
\end{subfigure}

\caption{Certification visualizations for Brain MRI samples using ResNet-18. 
Each row corresponds to one sample. Within each image (left to right): input image, Integrated Gradients, Grad-CAM, RISE, Occlusion, and LRP.}
\label{fig:brain_attr}
\end{figure}

\subsubsection{Chest X-ray}

\begin{figure}[H]
\centering

\begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=0.8\linewidth]{certifcation/chest/resnet18_img117_paper_style.png}
    \caption{Sample 1}
\end{subfigure}


\begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=0.8\linewidth]{certifcation/chest/resnet18_img31_paper_style.png}
    \caption{Sample 2}
\end{subfigure}

\caption{Certification visualizations for an ChestXray images using ResNet-18.}
\label{fig:aptos_attr}
\end{figure}




\subsubsection{APTOS 2019 (DR)}

\begin{figure}[H]
\centering

\begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=0.8\linewidth]{certifcation/fundus/resnet18_img146_paper_style.png}
    \caption{Sample 1}
\end{subfigure}

\begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=0.8\linewidth]{certifcation/fundus/resnet18_img65_paper_style.png}
    \caption{Sample 2}
\end{subfigure}
\caption{Certification visualizations for an APTOS 2019 (DR) sample using ResNet-18.}
\label{fig:isic_attr}
\end{figure}

\subsubsection{ISIC Skin Lesion }

\begin{figure}[H]
\centering

\begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=0.8\linewidth]{certifcation/isic/resnet18_img185_paper_style.png}
    \caption{Sample 1}
\end{subfigure}

\begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=0.8\linewidth]{certifcation/isic/resnet18_img371_paper_style.png}
    \caption{Sample 2}
\end{subfigure}
\caption{Certification visualizations for an ISIC Skin Lesion sample using ResNet-18.}
\label{fig:isic_attr}
\end{figure}

\subsection{Experiment 1 for Pixel-Level Certification: Cross-Architecture Comparison}

Experiment 1 evaluates all 4 architectures on a smaller subset (5 images per dataset, 20 total) using standard settings ($\sigma=1$, $N=50$, Integrated Gradients) to understand architecture effects on certification. 

\begin{table}[h!]
\centering
\caption{Experiment 1: Average certification accuracy (\%) across datasets and models for different top-$K$ certified pixel fractions.}
\label{tab:exp1_cert}
\resizebox{\textwidth}{!}{
\begin{tabular}{llccc ccc ccc ccc}
\toprule
& & \multicolumn{3}{c}{\textbf{Chest X-ray}} 
& \multicolumn{3}{c}{\textbf{Brain MRI}} 
& \multicolumn{3}{c}{\textbf{ISIC Skin}} 
& \multicolumn{3}{c}{\textbf{Fundus}} \\
\cmidrule(lr){3-5} \cmidrule(lr){6-8} \cmidrule(lr){9-11} \cmidrule(lr){12-14}
\textbf{Dataset} & \textbf{Model} 
& $K=5$ & $K=25$ & $K=50$
& $K=5$ & $K=25$ & $K=50$
& $K=5$ & $K=25$ & $K=50$
& $K=5$ & $K=25$ & $K=50$ \\
\midrule

\multirow{4}{*}{All} 
& ResNet-18     
& 92.90 & 49.71 & 36.51 
& 93.28 & 613 & 513 
& 89.84 & 45.36 & 31.97 
& 79.95 & 35.90 & 314 \\

& DenseNet-121  
& 91.24 & 35.81 & 25.00 
& 94.48 & 62.35 & 49.00 
& 92.20 & 41.57 & 30.26 
& 90.91 & 32.33 & 16.00 \\

& MobileNetV2   
& 89.87 & 40.97 & 24.37 
& 93.65 & 64.80 & 56.03 
& 84.97 & 44.85 & 33.38 
& 90.08 & 43.26 & 29.31 \\

& EfficientNet-B1
& 90.05 & 50.90 & 35.63 
& 92.13 & 55.62 & 42.68 
& 88.52 & 45.97 & 36.72 
& 89.73 & 42.37 & 26.47 \\

\bottomrule
\end{tabular}
}
\end{table*}
As expected, certification accuracy decreases monotonically as the certified pixel fraction $K$ increases, reflecting the increasing difficulty of guaranteeing robustness for larger attribution regions.Among all, Brain MRI consistently achieves the highest certification rates, while fundus and ISIC exhibit lower robustness due to class difference, color and texture complexity. 


\begin{table}[h!]
\centering
\caption{Experiment 1: Average percentage of certified pixels across datasets (all models combined).}
\label{tab:exp1_dataset_avg}
\begin{tabular}{lcc}
\toprule
\textbf{Dataset} & \textbf{Mean Certified (\%)} & \textbf{Total Images} \\
\midrule
Brain MRI   & 67.96 & 300 \\
ISIC Skin  & 55.47 & 300 \\
Chest X-ray & 55.25 & 300 \\
Fundus     & 518 & 300 \\
\midrule
\textbf{Overall} & \textbf{57.31} & \textbf{1200} \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[h!]
\centering
\caption{Experiment 1: Average percentage of certified pixels per model and dataset.}
\label{tab:exp1_model_avg}
\resizebox{\textwidth}{!}{
\begin{tabular}{lcccc}
\toprule
\textbf{Dataset} & \textbf{ResNet-18} & \textbf{DenseNet-121} & \textbf{MobileNetV2} & \textbf{EfficientNet-B1} \\
\midrule
Brain MRI   & 68.28 & 68.61 & \textbf{71.49} & 63.47 \\
Chest X-ray & \textbf{59.70} & 518 & 51.74 & 58.86 \\
ISIC Skin  & 55.72 & 54.67 & 54.40 & \textbf{57.07} \\
Fundus     & 48.83 & 46.41 & \textbf{54.22} & 52.86 \\
\bottomrule
\end{tabular}
}
\end{table}

\subsection{Experiment 2: Large-Scale Certification with ResNet-18}

Experiment 2 extends the analysis of Experiment 1 by scaling the certification evaluation to a substantially larger number of samples while fixing the model architecture. Based on the stability, competitive performance, and computational efficiency observed in Experiment 1, \textbf{ResNet-18} was selected as the backbone model for all datasets.

While Experiment 1 evaluated certification behavior on a limited subset of images ($N=5$ per model–dataset pair), Experiment 2 increases the number of evaluated samples to up to $N=100$ images per dataset, resulting in a total of 6070 certified samples across all datasets. All certification hyperparameters, attribution methods, and noise settings are kept identical to Experiment 1 to ensure a controlled comparison.

\begin{table}[H]
\centering
\caption{Experiment 2: Average certification accuracy (\%) using ResNet-18 across datasets for different certified pixel fractions.}
\label{tab:exp2_kwise}
\begin{tabular}{lccc}
\toprule
\textbf{Dataset} & $K=5$ & $K=25$ & $K=50$ \\
\midrule
Brain MRI   & 93.49 & 61.31 & 49.57 \\
Chest X-ray & 92.26 & 49.37 & 36.80 \\
Fundus     & 76.69 & 36.19 & 29.83 \\
ISIC Skin  & 89.92 & 41.99 & 317 \\
\bottomrule
\end{tabular}
\end{table}

Consistent with Experiment 1, certification accuracy decreases monotonically as the certified pixel fraction $K$ increases. Brain MRI remains the most robust dataset, while Fundus and ISIC exhibit lower certification rates due to higher visual complexity and color variation.

\begin{table}[H]
\centering
\caption{Experiment 2: Dataset-level average percentage of certified pixels using ResNet-18 across all attribution methods.}
\label{tab:exp2_dataset_avg}
\begin{tabular}{lc}
\toprule
\textbf{Dataset} & \textbf{Mean Certified (\%)} \\
\midrule
Brain MRI   & 68.12 \\
Chest X-ray & 59.48 \\
ISIC Skin  & 54.20 \\
Fundus     & 47.57 \\
\midrule
\textbf{Overall} & \textbf{57.31} \\
\bottomrule
\end{tabular}
\end{table}


\subsection{Evaluation: Robustness and Faithfullness}

\subsubsection{Robustness Via \%Certified}
\subsubsection{Faithfullness Via Certfied Pixel Deletion}


\subsubsection{Experiment 1: Robustness}
\begin{figure}[h!]
\centering

\begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=0.8\linewidth]{experiment1/robustness/brain_mri/resnet18/figures/robustness_stacked.png}
    \caption{Brain MRI}
\end{subfigure}

\begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=0.8\linewidth]{experiment1/robustness/chestxray/resnet18/figures/robustness_stacked.png}
    \caption{ChestXray}
\end{subfigure}


\begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=0.8\linewidth]{experiment1/robustness/fundus/resnet18/figures/robustness_stacked.png}
    \caption{APTOS 2019}
\end{subfigure}


\begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=0.8\linewidth]{experiment1/robustness/isic/resnet18/figures/robustness_stacked.png}
    \caption{Skin Lession Dataset}
\end{subfigure}

\caption{Robustness certification results for the dataset across four Resnet18 architecture for 5 images. 
Each row corresponds to one dataset. Bars indicate the proportion of certified, uncertified, and abstained samples for different attribution methods and certified pixel fractions $K=\{5\%,25\%,50\%\}$.}
\label{fig:robustness_brain}
\end{figure}



\begin{figure}[H]
    \centering
    \includegraphics[width=1\linewidth]{experiment1/avg_robustness.png}
    \caption{Average Pixel Certified across dataset}
    \label{fig:chestxray}
\end{figure}


\begin{figure}[h!]
\centering

\begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=1\linewidth]{experiment1/faithfulness/mri/faithfulness_confidence_curves.png}
    \caption{Brain MRI}
\end{subfigure}

\begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=1\linewidth]{experiment1/faithfulness/chest/faithfulness_confidence_curves.png}
    \caption{ChestXray}
\end{subfigure}


\begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=1\linewidth]{experiment1/faithfulness/fundus/faithfulness_confidence_curves.png}
    \caption{APTOS 2019}
\end{subfigure}


\begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=1\linewidth]{experiment1/faithfulness/isic/faithfulness_confidence_curves.png}
    \caption{Skin Lession Dataset}
\end{subfigure}

\caption{Faitfullness Curve  results for the dataset across four Resnet18 architecture for 5 images at $K=\{5\%,25\%,50\%\}$.}
\label{fig:robustness_brain}
\end{figure}

\textbf{Average Faithfullness across dataset}


\begin{figure}[H]
    \centering
    \includegraphics[width=1\linewidth]{experiment1/faithfulness/overall_confidence_curves.png}
    \caption{Average Confidence drop across dataset with Resenet18 Model for experiment 1}
    \label{fig:chestxray}
\end{figure}


% EXPERIMENT 2


\subsubsection{Experiment 2 Evaluation}
\begin{figure}[H]
\centering

\begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=0.8\linewidth]{experiment2/robustness/mri/robustness_stacked.png}
    \caption{Brain MRI}
\end{subfigure}

\begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=0.8\linewidth]{experiment2/robustness/chest/robustness_stacked.png}
    \caption{ChestXray}
\end{subfigure}


\begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=0.8\linewidth]{experiment2/robustness/fundus/robustness_stacked.png}
    \caption{APTOS 2019}
\end{subfigure}


\begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=0.8\linewidth]{experiment2/robustness/isic/robustness_stacked.png}
    \caption{Skin Lession Dataset}
\end{subfigure}

\caption{Robustness certification results for the dataset across four Resnet18 architecture for 5 images. 
Each row corresponds to one dataset. Bars indicate the proportion of certified, uncertified, and abstained samples for different attribution methods and certified pixel fractions $K=\{5\%,25\%,50\%\}$.}
\label{fig:robustness_brain}
\end{figure}



\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{experiment2/robustness/summary_mean_pct_certified.png}
    \caption{Average Pixel Certified across dataset}
    \label{fig:chestxray}
\end{figure}



\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{experiment2/robustness/method_by_dataset.png}
    \caption{Average Pixel Certified across dataset - Attribution Method Di}
    \label{fig:chestxray}
\end{figure}


\begin{figure}[h!]
\centering

\begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=0.8\linewidth]{experiment2/faithfulness/mri/faithfulness_confidence_curves.png}
    \caption{Brain MRI}
\end{subfigure}

\begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=0.8\linewidth]{experiment2/faithfulness/chest/faithfulness_confidence_curves.png}
    \caption{ChestXray}
\end{subfigure}


\begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=0.8\linewidth]{experiment2/faithfulness/fundus/faithfulness_confidence_curves.png}
    \caption{APTOS 2019}
\end{subfigure}


\begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=0.8\linewidth]{experiment2/faithfulness/isic/faithfulness_confidence_curves.png}
    \caption{Skin Lession Dataset}
\end{subfigure}

\caption{Faitfullness Curve  results for the dataset across four Resnet18 architecture for 5 images at $K=\{5\%,25\%,50\%\}$.}
\label{fig:robustness_brain}
\end{figure}

\textbf{Average Faithfullness across dataset}


\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{experiment2/faithfulness/isic/faithfulness_confidence_curves.png}
    \caption{Average Confidence drop across dataset with Resenet18 Model for experiment 2}
    \label{fig:chestxray}
\end{figure}



\paragraph{Effect of Increasing the Number of Samples.}
Increasing the number of evaluated samples in Experiment~2 did not lead to a qualitative change in the observed certification trends. The relative robustness ordering across datasets remained consistent with Experiment~1, with Brain MRI exhibiting the highest certification rates, followed by Chest X-ray and ISIC, and Fundus remaining the most challenging. Similarly, the monotonic decrease in certification as the certified pixel fraction $K$ increased was preserved. This indicates that the robustness patterns identified in the small-scale setting generalize to larger evaluations and are not artifacts of limited sample selection.


\newpage

\section{Grid-Based Localization: Implementation Details}

This section presents the grid-based evaluation framework for testing whether certified attributions correctly localize to known target regions. Unlike the standard medical datasets, grid images provide controlled ground truth where the target region is precisely defined. We describe the grid dataset construction algorithm, the model reuse strategy, the DiFull attribution wrapper, and the complete certification workflow.

\subsection{Overview: Model Reuse and Grid Evaluation}

\paragraph{Key insight.} We \textbf{do not train a separate model on grid images}. Instead, we reuse the ResNet-18 checkpoint trained on individual ISIC lesion images ($224 \times 224$). This checkpoint is loaded into a multi-head architecture where each head classifies one grid cell. During attribution, we apply the DiFull wrapper to process the full grid through the backbone while using the target cell's head for prediction. This tests whether certified attributions naturally localize to the correct cell based on the model's learned features, without grid-specific training.

\subsection{Algorithm 5: Grid Dataset Construction}

Grid images are synthetic $2 \times 2$ compositions of individual lesion images from different classes. Each grid has a known target cell (top-left) whose class determines the grid's label.

\begin{algorithm}[H]
\caption{Grid Dataset Generation}
\label{alg:grid_generation}
\begin{algorithmic}[1]
\REQUIRE Base dataset $D$ (ISIC), number of grids $N$, scale $m=2$, target cell index $t=0$
\ENSURE Grid dataset $\{G_i, y_i, M_i\}_{i=1}^N$ with images, labels, and ground-truth masks
\STATE \textbf{// Step 1: Organize base dataset by class}
\STATE Build class pools: $P_c \leftarrow \{i \mid D[i].\text{label} = c\}$ for each class $c$
\STATE Shuffle each pool: $P_c \leftarrow \text{shuffle}(P_c)$
\STATE \textbf{// Step 2: Generate each grid}
\FOR{$i = 1$ to $N$}
    \STATE \textbf{// Sample classes}
    \STATE $\mathcal{C} \leftarrow$ sample $m \times m$ distinct classes without replacement
    \STATE Randomly permute $\mathcal{C}$ and assign to cells: $\{c_0, c_1, c_2, c_3\}$
    \STATE \textbf{// Set target}
    \STATE $y_i \leftarrow c_t$ \quad // Label = class in target cell (top-left, $t=0$)
    \STATE \textbf{// Build grid image}
    \STATE Initialize $G_i \in \mathbb{R}^{3 \times 448 \times 448}$
    \FOR{cell $j = 0$ to $m^2-1$}
        \STATE Pop image index from pool: $\text{idx} \leftarrow P_{c_j}.\text{pop}()$
        \STATE Load image: $x \leftarrow D[\text{idx}].\text{image}$ \quad // $224 \times 224$
        \STATE Compute cell position: $(r, c) = (j \div m, j \mod m)$
        \STATE Place in grid: $G_i[:, r \cdot 224:(r+1) \cdot 224, c \cdot 224:(c+1) \cdot 224] \leftarrow x$
    \ENDFOR
    \STATE \textbf{// Create ground-truth mask}
    \STATE $M_i \in \{0,1\}^{448 \times 448}$, all zeros
    \STATE $M_i[0:224, 0:224] \leftarrow 1$ \quad // Mark target cell (top-left)
\ENDFOR
\RETURN $\{(G_i, y_i, M_i)\}_{i=1}^N$
\end{algorithmic}
\end{algorithm}

\paragraph{Implementation notes.}
\begin{itemize}
\item \textbf{No confidence filtering:} Images are sampled randomly from class pools, not based on model confidence.
\item \textbf{Sampling with replacement:} If a class pool is exhausted, it is refilled and reshuffled.
\item \textbf{Fixed target cell:} Cell 0 (top-left) is always the target across all grids.
\item \textbf{Class diversity:} Each grid uses 4 distinct classes unless the dataset has fewer than 4 classes.
\end{itemize}

\paragraph{Dataset statistics.}
We generate 100 grid images per split (train/val/test). Each grid is $448 \times 448$ pixels with four $224 \times 224$ cells. For ISIC (7 classes), grids sample 4 distinct lesion types per composition.

\subsection{Algorithm 6: Multi-Head Model with Checkpoint Reuse}

Instead of training a model on grid images, we load a pre-trained single-head ResNet-18 checkpoint (trained on individual $224 \times 224$ lesion images) into a multi-head architecture.

\begin{algorithm}[H]
\caption{Multi-Head Model Initialization with Checkpoint Reuse}
\label{alg:multihead_init}
\begin{algorithmic}[1]
\REQUIRE Pre-trained checkpoint $\theta_{\text{single}}$ (ResNet-18 on ISIC), number of heads $H=4$, number of classes $C$
\ENSURE Multi-head model $f_{\text{multi}}$ with shared backbone and $H$ classification heads
\STATE \textbf{// Step 1: Initialize multi-head architecture}
\STATE $f_{\text{multi}} \leftarrow$ GridMultiHead(backbone=ResNet-18, num\_heads=$H$, num\_classes=$C$)
\STATE \textbf{// Step 2: Load pre-trained backbone weights}
\STATE Load checkpoint: $\theta_{\text{single}} \leftarrow$ torch.load(checkpoint\_path)
\STATE Extract backbone weights: $\theta_{\text{backbone}} \leftarrow \{\text{k: v for k, v in } \theta_{\text{single}} \text{ if "backbone" in k}\}$
\STATE Update model: $f_{\text{multi}}.\text{backbone.load\_state\_dict}(\theta_{\text{backbone}})$
\STATE \textbf{// Step 3: Initialize all heads identically}
\STATE Extract head weights: $\theta_{\text{head}} \leftarrow \{\text{k: v for k, v in } \theta_{\text{single}} \text{ if "classifier" in k}\}$
\FOR{$h = 0$ to $H-1$}
    \STATE $f_{\text{multi}}.\text{heads}[h].\text{load\_state\_dict}(\theta_{\text{head}})$
\ENDFOR
\RETURN $f_{\text{multi}}$
\end{algorithmic}
\end{algorithm}

\paragraph{Critical distinction.}
\begin{itemize}
\item \textbf{No grid training:} The model is \emph{not} trained on grid images. All weights come from training on individual lesion images.
\item \textbf{Zero-shot grid evaluation:} The model processes grid cells using features learned from single-image classification.
\item \textbf{Multi-head purpose:} Each head is associated with one grid cell. During attribution, we use head 0 (target cell) for logits.
\end{itemize}

\subsection{Algorithm 7: DiFull Attribution Wrapper}

The DiFull wrapper modifies how the model processes grid images during attribution. Unlike cropping (which was never used during our evaluation), it passes the full grid through the backbone and extracts logits from the target head.

\begin{algorithm}[H]
\caption{DiFull Attribution Wrapper}
\label{alg:difull_wrapper}
\begin{algorithmic}[1]
\REQUIRE Multi-head model $f_{\text{multi}}$, target head index $h_t=0$, full grid image $G \in \mathbb{R}^{3 \times 448 \times 448}$
\ENSURE Logits $z \in \mathbb{R}^C$ for target cell
\STATE \textbf{// Forward pass: full grid through backbone}
\STATE $\phi(G) \leftarrow f_{\text{multi}}.\text{backbone}(G)$ \quad // Global features from full $448 \times 448$ grid
\STATE \textbf{// Apply target head}
\STATE $z \leftarrow f_{\text{multi}}.\text{heads}[h_t](\phi(G))$ \quad // Logits from head 0
\RETURN $z$
\end{algorithmic}
\end{algorithm}

\paragraph{Why DiFull enables localization testing.}
\begin{itemize}
\item \textbf{Gradient flow:} Since the full grid enters the backbone, gradients $\nabla_G z$ can flow to \emph{all} pixels.
\item \textbf{Learned localization:} The backbone was trained on individual lesions, so its features naturally respond most strongly to lesion-like patterns. When the target head is applied to full-grid features, attribution should concentrate on the target cell (which contains a lesion) and ignore other cells.
\item \textbf{Controlled ground truth:} We know the correct region (top-left cell). GridPG measures whether certified pixels align with this known target.
\end{itemize}

\subsection{Algorithm 8: Grid Certification Workflow}

The complete pipeline combines grid dataset generation, DiFull attribution, and the certification framework from Section 3.2.

\begin{algorithm}[H]
\caption{Grid-Based Certified Attribution Evaluation}
\label{alg:grid_certification}
\begin{algorithmic}[1]
\REQUIRE Pre-trained checkpoint $\theta$, base dataset $D$, grid count $N$, certification params $(\sigma, n, \tau, K)$
\ENSURE GridPG scores for each attribution method
\STATE \textbf{// Step 1: Generate grid dataset}
\STATE $\{(G_i, y_i, M_i)\}_{i=1}^N \leftarrow$ GenerateGrids$(D, N, m=2, t=0)$ \quad // Algorithm 5
\STATE \textbf{// Step 2: Load model with checkpoint reuse}
\STATE $f_{\text{multi}} \leftarrow$ LoadMultiHead$(\theta, H=4, C)$ \quad // Algorithm 6
\STATE \textbf{// Step 3: Wrap model for DiFull attribution}
\STATE $f_{\text{DiFull}} \leftarrow$ DiFull\_Wrapper$(f_{\text{multi}}, h_t=0)$ \quad // Algorithm 7
\STATE \textbf{// Step 4: For each grid, certify attributions}
\FOR{$i = 1$ to $N$}
    \STATE $G, y, M \leftarrow G_i, y_i, M_i$
    \FOR{each attribution method $h$ (IG, Grad-CAM, LRP, RISE, Occlusion)}
        \STATE \textbf{// Compute base attribution (full grid)}
        \STATE $a(G) \leftarrow h(f_{\text{DiFull}}, G, y)$ \quad // Heatmap $\in \mathbb{R}^{448 \times 448}$
        \STATE \textbf{// Apply randomized smoothing certification (Algorithm 2 + 3)}
        \STATE $p_1, p_0 \leftarrow$ RandomizedSmoothing$(G, h, K, \sigma, n)$
        \STATE $c(G) \leftarrow$ Certify$(p_1, p_0, \tau)$ \quad // Ternary map: $\{0, 1, \text{abstain}\}$
        \STATE \textbf{// Compute GridPG localization score}
        \STATE $S_{\text{target}} \leftarrow \sum_{p \in M} \mathbb{1}[c(G)_p = 1]$ \quad // Certified-1 pixels in target cell
        \STATE $S_{\text{total}} \leftarrow \sum_{p} \mathbb{1}[c(G)_p = 1]$ \quad // All certified-1 pixels
        \STATE $\text{GridPG}_{i,h,K} \leftarrow S_{\text{target}} / \max(S_{\text{total}}, 1)$
    \ENDFOR
\ENDFOR
\STATE \textbf{// Step 5: Aggregate results}
\STATE Compute mean GridPG per method and $K$ across all grids
\RETURN GridPG scores by method and sparsity level
\end{algorithmic}
\end{algorithm}

\subsection{GridPG Metric: Interpretation}

The Certified GridPG score quantifies localization accuracy:
$$
\text{GridPG} = \frac{\#\{\text{certified-1 pixels in target cell}\}}{\#\{\text{all certified-1 pixels}\}}
$$

\paragraph{Interpretation ranges.}
\begin{itemize}
\item \textbf{GridPG = 1.0:} Perfect localization — all certified important pixels are in the target cell.
\item \textbf{GridPG = 0.25:} Random localization — certified pixels are uniformly distributed across 4 cells (expected for uninformed attribution).
\item \textbf{GridPG < 0.25:} Misleading — certified pixels concentrate in \emph{wrong} cells, worse than random.
\item \textbf{GridPG > 0.25:} Localization bias toward target cell, increasing confidence with higher scores.
\end{itemize}

\subsection{Summary: Grid Workflow vs. Standard Dataset Workflow}

\begin{table}[h!]
\centering
\caption{Comparison of standard dataset and grid evaluation workflows.}
\label{tab:grid_vs_standard}
\small
\begin{tabular}{lll}
\toprule
\textbf{Component} & \textbf{Standard Datasets} & \textbf{Grid Evaluation} \\
\midrule
Input images & Individual lesions ($224 \times 224$) & Synthetic grids ($448 \times 448$) \\
Model training & ResNet-18 on lesion images & \textbf{No training} (reuse checkpoint) \\
Model architecture & Single-head classifier & Multi-head (4 heads, 1 per cell) \\
Attribution input & Single lesion image & Full grid via DiFull wrapper \\
Ground truth & Unknown (qualitative only) & Known target cell (top-left) \\
Evaluation metric & \% Certified pixels & GridPG (localization accuracy) \\
Certification & Same (Algorithm 2 + 3) & Same (Algorithm 2 + 3) \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Key implementation details.}
\begin{enumerate}
\item \textbf{No grid-specific training:} Model weights come entirely from single-image training.
\item \textbf{Random class assignment:} Grid cells are filled with randomly sampled classes, not based on model confidence.
\item \textbf{DiFull attribution:} Full grid enters backbone; gradients flow to all cells; target head extracts logits.
\item \textbf{Unified certification:} Same sparsification + smoothing + certification pipeline (Section 3.2) applied to grid heatmaps.
\end{enumerate}


\newpage


\subsubsection{Certified Grid Evaluation (GridPG)}
To evaluate localization, we construct grid images and measure where certified important pixels fall. Each grid image is split into $m\times m$ cells (here $m=2$), and the target cell is fixed (top-left). The Certified GridPG score is the fraction of certified-1 pixels that fall inside the target cell:
$$
\text{GridPG} = \frac{\#\{\text{certified-1 pixels in target cell}\}}{\#\{\text{all certified-1 pixels}\}}.
$$
If no certified-1 pixels exist, the score is set to 0. This evaluates whether certified important pixels align with the correct region rather than simply being robust.

\paragraph{Grid dataset workflow.} We build $2\times2$ grids from ISIC images, placing a different class in each cell. The target cell is index 0 (top-left), and the target class is the class placed there. We generate 100 grid images per split and derive a binary target mask for the target cell. These grids are then passed through the same attribution + smoothing pipeline, and the certified maps are scored using GridPG.

\paragraph{DiFull-style attribution on grids.} For grid evaluation we use a multi-head classifier with one head per cell. During attribution, the full grid is processed and the target-cell head is used for logits, so gradients can flow across the entire grid while preserving the target cell as the prediction source. Certified maps are computed for $K\in\{50,25,5\}$ with the same $(\sigma,\tau,n)$ used in the main experiments.




\end{document}